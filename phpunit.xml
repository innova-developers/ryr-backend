<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <!-- RC-533: sin esto la suite toma la instancia de GreenAPI del .env local y cada alta
             de comisión de los tests le escribe por WhatsApp a un teléfono de faker. -->
        <env name="WHATSAPP_API_INSTANCE" value=""/>
        <env name="WHATSAPP_API_TOKEN" value=""/>
        <!-- Los tests trabajan con fakes; el sandbox de envíos se prueba aparte (SandboxEnviosTest).
             "0" y no "false": PHPUnit convierte "false" en booleano y la variable llega vacía,
             que SandboxEnvios toma como "no configurado" y prende el sandbox. -->
        <env name="SANDBOX_ENVIOS" value="0"/>
        <!-- Sin credenciales de Firebase, como en la CI: el .env local puede tener las reales. -->
        <env name="FIREBASE_CREDENTIALS_PATH" value="/tmp/sin-firebase-en-tests.json"/>
        <!-- RC-549: cada Location::factory() geocodificaba contra Nominatim real (1.751 llamadas,
             todas 429, en las corridas del 24/09). Los tests que lo necesitan lo prenden y usan Http::fake. -->
        <env name="NOMINATIM_GEOCODE_ON_SAVE" value="false"/>
    </php>
</phpunit>
